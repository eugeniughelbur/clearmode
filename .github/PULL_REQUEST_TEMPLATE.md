## What changed

<!-- One or two sentences. -->

## Rule pack

- [ ] I changed `rules/clear-100.json`, not a file under `targets/`
- [ ] I ran `python3 scripts/compile_targets.py`
- [ ] `python3 scripts/compile_targets.py --check` passes
- [ ] `python3 -m unittest discover tests` passes

## If this changes a rule

Paste one sentence that now passes and one that now fails.
